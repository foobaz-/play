# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           main.controllers.EntryPoint.index

# Login
GET     /login                      main.controllers.UserManager.showLoginScreen
GET     /register                   main.controllers.UserManager.showRegisterScreen
POST    /newuser                    main.controllers.UserManager.newUser
POST    /loginas                    main.controllers.UserManager.login

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
